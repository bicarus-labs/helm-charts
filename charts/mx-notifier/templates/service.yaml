apiVersion: v1
kind: Service
metadata:
  name: {{ include "mx-notifier.fullname" . }}
  labels:
    {{- include "mx-notifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mx-notifier.selectorLabels" . | nindent 4 }}
